program @Main
var @first
if @first - 1 equals @first * 5
then
	do while (@first lessthn 100 + 2 + (2 * 2))
    	read ( @first )
    	write ( @first )
    enddo
    read( @first )
    write( @first )
fi
endprogram